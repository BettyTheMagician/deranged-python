   1. Save the script as install.sh in a directory on your system.
   2. Make the script executable by running chmod +x install.sh in the terminal.
   3. Run the script by running ./install.sh in the terminal.
